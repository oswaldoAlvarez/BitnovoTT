import '../global.css';
import '@/core/devtools/reactotron';

import { RootLayoutNavigation } from '@/core/navigation/root-layout-navigation';

const RootLayout = () => {
  return <RootLayoutNavigation />;
};

export default RootLayout;
